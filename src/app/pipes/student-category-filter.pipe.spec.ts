import { StudentCategoryFilterPipe } from './student-category-filter.pipe';

describe('StudentCategoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentCategoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
